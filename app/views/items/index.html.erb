<h1><%= current_user.name %>'s To-do Lists</h1>

<%= render "items/form" %>

<% if @items.any? %>
<table class="table">
  <%= render @items %>
</table>
<% else %>
  <p>You have no items in your list</p>
<% end %>
